# 约会邀请 MVP 产品设计

## 目标
生成一个可以发给暧昧对象的网页邀请。对方无需注册即可回应，结果自动保存，发起人通过受密码保护的管理页查看。

## 邀请页
- 移动端优先，暖白和浅粉配色，文案轻松、不过分亲密，使用通用称呼。
- 标题：这里有一份只发给你的邀请。
- 正文：想约你一起吃点好吃的，有空一起吗？
- 活动固定为“吃点好吃的”，不提供活动选择。
- 回应按钮：“好呀”和“这次先不了”，两者都可以正常选择。
- 选择“好呀”后，展示一个必填时间文本框，提示：例如周六下午，或者时间再商量。时间由对方填写，不预设日期。
- 选择“这次先不了”无需填写时间或理由。
- 点击“把回应送给你”后提交。提交前显示：提交后，对方会看到你的选择。
- 保存成功后，答应显示“收到，已经开始期待了！”，婉拒显示“收到，谢谢你的回应。”
- 保存失败时保留填写内容，提示重试；提交过程中禁用提交按钮，避免重复点击。

## 保存与查看
- 使用服务端持久化存储，不仅保存在浏览器本地。
- 保存字段：回应（答应或婉拒）、方便的时间（婉拒时为空）、提交时间。
- 管理页通过密码验证后展示回应列表，按提交时间倒序排列。
- 读取回应必须经过服务端鉴权，不能仅隐藏管理页入口。管理密码放在服务端环境变量中，不写入前端或 Git 仓库。
- MVP 使用一个邀请链接，不识别提交者身份，不区分多位收件人。

## 不做的功能
拆信封、翻卡片、活动选择、日期选择器、留言、庆祝动画、账号注册、自动通知和复杂后台。

## 验收与测试
- 手机端可完整查看并操作邀请页。
- 答应时必须填写非空时间文本；可填写“时间再商量”。
- 婉拒可直接提交，切换到婉拒后不保存此前填写的时间。
- 提交成功后刷新页面或重启服务，管理页仍能查看已保存结果。
- 未验证密码时无法通过管理页或接口读取结果。
- 保存失败时不显示成功文案，允许重试。
- 实现代码时编写或更新上述核心流程的测试，全部通过后创建 Git commit 并推送到 GitHub。
